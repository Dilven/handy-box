import Head from 'next/head'
import Image from 'next/image'
import { useState } from 'react'
import { useQuery } from 'react-query'
import { InternalApi } from '../services/internal-api'
import styles from '../styles/Home.module.css'

const favouritePlaces = [
  'Wrocław',
  'Wieluń'
]

export default function Home() {
  const { data } = useQuery('weather', InternalApi.getWeather);
  const [ savedPlaces ] = useState(favouritePlaces);
  return (
    <div className={styles.container}>
      <Head>
        <title>Handy-box</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon-husky.png" />
      </Head>
      <main className={styles.main}>
        <img style={{ height: '150px', marginTop: '50px'}} src="/logo-husky.png" alt="logo-husky"></img>
        <h1 className={styles.title}>
          Welcome to handy-box!
        </h1>
        <p className={styles.description}>
          Create your place
        </p>

        <div className={styles.grid}>
          <div className={styles.card}>
            <h2>Weather</h2>
            <p>Favourit place: {savedPlaces.map(place => <button>{place}</button>)}</p>
            <br />
            <input type='text'></input>
            {data && (
              <div>
                <p>temp: {data.main.temp}</p>
                <p>temp_max: {data.main.temp_max}</p>
                <p>temp_min: {data.main.temp_min}</p>
                <p>feels_like: {data.main.feels_like}</p>
                <p>humidity: {data.main.humidity}</p>
                <p>pressure: {data.main.pressure}</p>
              </div>
            )
            }
          </div>
          <div className={styles.card}>
            <h2>Weather</h2>
          </div>
          <div className={styles.card}>
            <h2>Weather</h2>
          </div>
          <div className={styles.card}>
            <h2>Weather</h2>
          </div>
        </div>
      </main>

      <footer className={styles.footer}>
        <a
          href="https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          Powered by{' '}
          <span className={styles.logo}>
            <Image src="/vercel.svg" alt="Vercel Logo" width={72} height={16} />
          </span>
        </a>
      </footer>
    </div>
  )
}
